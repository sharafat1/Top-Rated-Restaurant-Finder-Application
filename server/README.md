# Running the Application

Step 1: Download the project form GitHub.

Step 2: Using command line, go to the server directory. Type ``install npm`` which will install all of the required packages and libraries. 

Step 3: After the installation of all packages and libraries, type the ``npm start`` this will compile the application. After this step the server will be running and serving all requests from front-end.